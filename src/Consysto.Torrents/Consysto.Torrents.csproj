<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <RootNamespace>Consysto.Torrents</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MonoTorrent" />
    <!-- Keeps MonoTorrent and Mono.Nat whole under Native AOT, see the file. -->
    <EmbeddedResource Include="ILLink.Descriptors.xml" LogicalName="ILLink.Descriptors.xml" />
  </ItemGroup>

</Project>
